<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahnuma.rahnuma">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- add these permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="Rahnuma Guide"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splah">

        </activity>
        <activity android:name=".guides_main" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".Main2Activity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".Main3Activity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".guides_login" />
        <activity android:name=".tours" />
        <activity android:name=".individual_tour" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>